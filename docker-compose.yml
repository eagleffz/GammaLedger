services:
  gammaledger:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - FINNHUB_API_KEY=${FINNHUB_API_KEY}
      - FINNHUB_RATE_LIMIT=60
      - DEFAULT_FEE_PER_CONTRACT=0.65
